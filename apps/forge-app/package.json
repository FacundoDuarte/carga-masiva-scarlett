{
  "name": "forge",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "lint": "npm-run-all 'lint:*'",
    "lint:eslint": "eslint . --quiet --ext ts,cjs,js --cache",
    "lint:types": "tsc --noEmit",
    "test": "jest",
    "deploy": "forge deploy",
    "deploy:prod": "forge deploy -e production"
  },
  "dependencies": {
    "@forge/resolver": "^1.5.25",
    "utils": "*",
    "manifest": "*",
    "web": "*",
    "remote-app": "*"
  },
  "devDependencies": {
    "@tsconfig/node18": "^18.2.2"
  }
}