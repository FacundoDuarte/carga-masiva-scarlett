.PHONY: build-ValidateSessionFunction

build-ValidateSessionFunction:
	mkdir -p $(ARTIFACTS_DIR)
	cp validate-session.mjs $(ARTIFACTS_DIR)/
	cp package.json $(ARTIFACTS_DIR)/
	cd $(ARTIFACTS_DIR) && bun install
