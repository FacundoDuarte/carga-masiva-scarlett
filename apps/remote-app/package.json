{
  "name": "remote-app",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "sam build && chmod +x ./scripts/deploy.sh && ./scripts/deploy.sh"
  },
  "dependencies": {
    "aws-lambda": "^1.0.7",
    "bun": "^1.2.2",
    "jose": "^5.9.6",
    "utils": "*"
  }
}