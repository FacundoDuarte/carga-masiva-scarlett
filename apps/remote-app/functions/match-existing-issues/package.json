{
  "name": "match-existing-issues",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "make build-match-existing-issuesFunction",
    "deploy": "echo \"Use SAM deploy from root directory\"",
    "build:deploy": "bun run build && bun run deploy",
    "install:deps": "bun install",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.147",
    "@types/node": "^22.13.4",
    "typescript": "^5.0.0",
    "esbuild": "0.19.12"
  }
}
