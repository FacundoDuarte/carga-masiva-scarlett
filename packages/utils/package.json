{
  "name": "utils",
  "main": "./src/index.ts",
  "module": "./src/index.ts",
  "types": "./src/index.ts",
  "type": "module",
  "scripts": {
    "install:deps": "bun install",
    "build": "bun build ./src/**/*.ts --outdir ./dist/utils --target node",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "aws-lambda": "^1.0.7",
    "jose": "^5.9.6"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/bun": "^1.2.2"
  }
}